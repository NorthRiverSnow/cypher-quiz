# pnpm は workspace をこのファイルで定義する（package.json の "workspaces" は読まない）
packages:
  - "packages/*"
catalog:
  vite: npm:@voidzero-dev/vite-plus-core@0.3.0
  vite-plus: 0.3.0
overrides:
  vite@*: "catalog:"
peerDependencyRules:
  allowAny:
    - vite
  allowedVersions:
    vite: "*"
